# Required: your backend API base URL (no trailing slash)
VITE_API_BASE_URL=https://your-api.example.com

# Optional: API path prefix (e.g. /api). Leave empty if the API is at the root.
VITE_API_PREFIX=
